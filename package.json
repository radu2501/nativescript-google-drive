{
  "name": "nativescript-google-drive",
  "version": "1.0.0",
  "description": "A Google Drive client for NativeScript (Android)",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typedoc": "rmdir /s /q docs && typedoc --tsconfig tsconfig.json --out .\\docs --mode file --excludePrivate --hideGenerator --excludeNotExported --name \"NativeScript Google Drive Client (Android only)\"",
    "build": "rmdir /s /q dist && tsc && yarn typedoc"
  },
  "repository": "https://github.com/radu2501/nativescript-google-drive",
  "keywords": [
    "nativescript",
    "google",
    "drive"
  ],
  "author": "Radu Nicoara",
  "license": "MIT",
  "nativescript": {
    "platforms": {
      "android": "2.3.0"
    }
  },
  "dependencies": {
    "nativescript-permissions": "^1.2.3"
  },
  "devDependencies": {
    "@types/node": "^8.0.34",
    "tns-core-modules": "^3.2.0"
  }
}
